# AWS Bedrock DeepSeek + Scalebox CSV 数据生成器

使用 AWS Bedrock 的 DeepSeek v3 模型生成 CSV 格式数据，并通过 Scalebox 沙箱进行安全存储和管理。

## 📋 项目简介

本示例展示如何利用 AI 生成高质量的 CSV 测试数据：
- 使用自然语言描述数据需求
- AI 自动生成符合要求的 CSV 数据
- 保存到 Scalebox 沙箱环境
- 验证数据格式和内容

## ✨ 核心功能

- ✅ **AI 驱动生成**：使用 Bedrock DeepSeek v3 模型
- ✅ **严格格式控制**：确保输出标准 CSV 格式
- ✅ **多样化数据**：支持用户、销售、成绩等各类数据
- ✅ **安全存储**：Scalebox 沙箱环境隔离
- ✅ **自动验证**：数据格式和内容检查
- ✅ **便于复用**：可用于测试、分析、机器学习等场景

## 🔄 工作流程

```
┌─────────────────────────────────────────────────────────────┐
│                    CSV 数据生成流程                           │
└─────────────────────────────────────────────────────────────┘

[1] 创建 Sandbox 实例
     ↓
[2] 定义数据生成任务
     - 使用自然语言描述需求
     - 指定字段、数量、数据类型
     ↓
[3] 调用 AWS Bedrock DeepSeek
     - 发送生成请求
     - 接收 AI 生成的 CSV 数据
     ↓
[4] 验证和清理数据
     - 移除 markdown 标记
     - 验证 CSV 格式
     ↓
[5] 保存到 Sandbox
     - 写入文件系统
     - 验证文件大小和行数
     ↓
[6] 数据统计
     - 显示数据预览
     - 输出统计信息
     ↓
✅ CSV 数据就绪，可用于后续分析
```

## 环境要求

### 安装依赖

```bash
pip install boto3 python-dotenv scalebox
```

### 环境变量配置

复制 `.env.example` 为 `.env` 并填写以下配置：

```bash
# Scalebox API 密钥
SBX_API_KEY="your_scalebox_api_key"

# AWS Bedrock 配置
AWS_REGION="eu-north-1"  # 根据你的模型所在区域设置

# 方式1：使用 Bearer Token（推荐用于 Bedrock）
AWS_BEARER_TOKEN_BEDROCK="your_bearer_token"

# 方式2：使用标准 AWS 凭证（可选）
# AWS_ACCESS_KEY_ID="your_aws_access_key"
# AWS_SECRET_ACCESS_KEY="your_aws_secret_key"
```

### 测试 AWS Bedrock 连接

在运行主程序之前，建议先测试 Bedrock 连接和认证：

```bash
python test_bedrock_auth.py
```

此脚本会：
1. 检查环境变量配置
2. 测试 Bedrock 客户端连接
3. 列出可用的模型
4. 尝试调用 DeepSeek 模型
5. 如果出错，提供诊断建议

### AWS Bedrock 权限

确保你的 AWS 账号/Token 具有以下权限：
- `bedrock:ListFoundationModels` - 列出模型
- `bedrock:InvokeModel` - 调用模型
- 访问 `deepseek.v3-v1:0` 模型的权限

**重要**: 需要在 AWS Console 的 Bedrock 服务中启用模型访问权限：
1. 进入 AWS Bedrock 控制台
2. 选择 "Model access"
3. 启用 "DeepSeek-V3.1" 模型

## 🚀 快速开始

### 运行示例

```bash
# 运行数据生成脚本
python run.py
```

### 自定义数据生成任务

编辑 `run.py` 中的 `data_tasks` 列表：

```python
data_tasks = [
    {
        "name": "学生成绩",
        "prompt": "生成40人班级期末考试成绩，包含：学号、姓名、语文、数学、英语、物理、化学、生物",
        "filename": "exam_scores.csv"
    },
    {
        "name": "用户数据",
        "prompt": "生成100条用户数据，包含：用户ID、姓名、年龄、性别、邮箱、注册日期",
        "filename": "users.csv"
    },
    {
        "name": "销售记录",
        "prompt": "生成50条销售记录，包含：订单ID、产品名称、数量、单价、总价、销售日期、销售员",
        "filename": "sales.csv"
    }
]
```

### 输出示例

```
2024-01-15 10:30:00 - INFO - 创建 Sandbox 实例...
2024-01-15 10:30:01 - INFO - Sandbox 创建成功，ID: sbx_abc123xyz
2024-01-15 10:30:01 - INFO - 
开始生成数据集: 学生成绩
2024-01-15 10:30:01 - INFO - 提示词: 生成40人班级期末考试成绩...
2024-01-15 10:30:02 - INFO - 调用 Bedrock 模型: deepseek.v3-v1:0
2024-01-15 10:30:05 - INFO - 成功收到 Bedrock 响应
2024-01-15 10:30:05 - INFO - 
生成的 CSV 数据预览（前500字符）:
学号,姓名,语文,数学,英语,物理,化学,生物
2024001,张三,88,92,85,90,87,89
2024002,李四,91,88,90,85,92,88
...
2024-01-15 10:30:05 - INFO - CSV 数据已保存到 Sandbox: /tmp/exam_scores.csv
2024-01-15 10:30:05 - INFO - 文件大小: 2048 字节
2024-01-15 10:30:05 - INFO - ✅ CSV 数据生成并存储成功！
```

### 从 Sandbox 下载数据

```python
# 从 Sandbox 读取数据
csv_content = sandbox.files.read("/tmp/users.csv")

# 保存到本地
with open("local_users.csv", "w", encoding="utf-8") as f:
    f.write(csv_content)

print("数据已下载到 local_users.csv")
```

## Sandbox 初始化说明

⚠️ **重要**：必须使用 `Sandbox.create()` 类方法创建沙箱实例。

```python
# ✅ 正确方式
sandbox = Sandbox.create()  # 使用默认配置
sandbox = Sandbox.create(template="base", timeout=600)  # 自定义配置

# ❌ 错误方式
sandbox = Sandbox()  # 会报错：KeyError: 'connection_config'
```

`Sandbox.create()` 方法支持的参数：
- `template`: 沙箱模板名称，默认 "base"
- `timeout`: 超时时间（秒），默认 300 秒
- `metadata`: 自定义元数据
- `envs`: 环境变量字典
- `api_key`: Scalebox API 密钥（默认从环境变量 `SBX_API_KEY` 读取）
- `request_timeout`: 请求超时时间
- `allow_internet_access`: 是否允许访问互联网，默认 `True`

## 核心函数说明

### `generate_csv_data_with_bedrock(prompt, model_id)`

调用 AWS Bedrock DeepSeek 模型生成 CSV 数据。

**参数：**
- `prompt` (str): 数据生成任务描述
- `model_id` (str): Bedrock 模型 ID，默认 "deepseek.v3-v1:0"

**返回：**
- `str`: CSV 格式的数据内容

**特点：**
- 自动添加严格的 CSV 格式约束
- 清理可能的 markdown 标记
- 完整的错误处理和日志

### `save_csv_to_sandbox(sandbox, csv_content, filename)`

将 CSV 数据保存到 Sandbox 文件系统。

**参数：**
- `sandbox` (Sandbox): Sandbox 实例
- `csv_content` (str): CSV 格式内容
- `filename` (str): 文件名，默认 "generated_data.csv"

**返回：**
- `str`: 保存的文件路径

**特点：**
- 自动保存到 `/tmp/` 目录
- 验证文件写入成功
- 返回完整文件路径

## Sandbox 接口使用

### 文件操作

```python
# 写入文件
sandbox.files.write("/tmp/data.csv", csv_content)

# 读取文件
content = sandbox.files.read("/tmp/data.csv")

# 列出文件
files = sandbox.files.list("/tmp")
```

### 命令执行

```python
# 列出文件
result = sandbox.commands.run("ls -lh /tmp/*.csv")
print(result.stdout)

# 统计行数
result = sandbox.commands.run("wc -l /tmp/data.csv")
print(result.stdout)

# 查看文件前几行
result = sandbox.commands.run("head -n 5 /tmp/data.csv")
print(result.stdout)
```

## 📋 数据用途

生成的 CSV 数据可用于：

1. **应用测试** - 提供真实感的测试数据
2. **数据分析练习** - 学习 pandas、数据可视化等技能
3. **机器学习** - 作为训练/测试数据集
4. **原型开发** - 快速生成演示数据
5. **性能测试** - 批量数据生成用于压力测试

### 简单分析示例

```python
# 在 Sandbox 中使用 pandas 分析
sandbox.commands.run("pip install pandas")

analysis_script = """
import pandas as pd
df = pd.read_csv('/tmp/exam_scores.csv')
print('数据形状:', df.shape)
print('\\n数据摘要:')
print(df.describe())
print('\\n前5行:')
print(df.head())
"""

sandbox.files.write("/tmp/analyze.py", analysis_script)
result = sandbox.commands.run("python /tmp/analyze.py")
print(result.stdout)
```

## 常见问题

### 1. AWS 认证失败：AccessDeniedException

**错误信息**：`Authentication failed: Please make sure your API Key is valid.`

**解决方法**：

1. **检查 Bearer Token**：
   ```bash
   # 确保在 .env 文件中配置了正确的 token
   AWS_BEARER_TOKEN_BEDROCK="your_token_here"
   ```

2. **检查 Region**：
   确保使用正确的 AWS region（从 check_bedrock.py 输出中确认）：
   ```bash
   AWS_REGION="eu-north-1"  # 或其他你的模型所在区域
   ```

3. **运行诊断脚本**：
   ```bash
   python test_bedrock_auth.py
   ```

4. **检查 AWS 凭证文件**（如果使用标准凭证）：
   ```bash
   cat ~/.aws/credentials
   cat ~/.aws/config
   ```

5. **Token 过期**：Bearer Token 可能有时效性，需要定期更新

### 2. Bedrock 模型访问权限

**错误信息**：模型调用失败或找不到模型

**解决方法**：

在 AWS Console 中启用 Bedrock 模型访问权限：
1. 进入 AWS Bedrock 控制台（正确的 region）
2. 选择 "Model access"
3. 找到并启用 "DeepSeek-V3.1" 模型
4. 等待状态变为 "Access granted"

### 3. Region 配置错误

**错误信息**：找不到模型或连接超时

**解决方法**：

不同的 region 支持的模型不同。运行以下脚本检查：
```bash
python check_bedrock.py
```

然后在 `.env` 中设置正确的 region：
```bash
AWS_REGION="eu-north-1"  # 使用你的模型所在 region
```

### 4. 生成的数据不是纯 CSV 格式

代码已包含自动清理逻辑，会移除 markdown 标记。如果仍有问题，可以调整 prompt 使其更严格。

### 5. 保留 Sandbox 用于后续操作

如果需要保留 Sandbox 继续使用，在 `run.py` 中注释掉清理代码：

```python
finally:
    # logger.info("\n[清理] 关闭 Sandbox...")
    # sandbox.kill()
    logger.info("\n保持 Sandbox 运行...")
    logger.info(f"Sandbox ID: {sandbox.sandbox_id}")
    logger.info("使用完毕后请手动调用 sandbox.kill() 清理资源")
```

## 最佳实践

1. **Prompt 设计**：明确指定字段名称、数据类型和数量
2. **数据验证**：生成后检查数据质量和格式
3. **资源管理**：使用完 Sandbox 后及时清理
4. **错误处理**：捕获并记录 API 调用错误
5. **成本控制**：合理控制生成的数据量

## 🏗️ 技术架构

```
┌───────────────────────────────────────────────────────────┐
│                    CSV 数据生成架构                         │
└───────────────────────────────────────────────────────────┘

┌──────────────────┐
│   run.py    │
│  (Python 脚本)   │
└────────┬─────────┘
         │
         ▼
    ┌────────────────────────────────────────┐
    │          AWS Bedrock API               │
    │      DeepSeek v3 模型                  │
    │  - 接收自然语言描述                     │
    │  - 生成 CSV 格式数据                   │
    └────────┬───────────────────────────────┘
             │
             ▼
    ┌──────────────────┐
    │   Scalebox       │
    │   Sandbox        │
    │  ├─ 文件存储     │
    │  ├─ 命令执行     │
    │  └─ 环境隔离     │
    └────────┬─────────┘
             │
             ▼
    ┌─────────────────────────────────────────┐
    │          输出结果                        │
    │  - CSV 数据文件 (/tmp/*.csv)            │
    │  - 数据验证和统计信息                    │
    │  - 可用于使用或下载                      │
    └─────────────────────────────────────────┘
```

## 许可证

请遵循 Scalebox 和 AWS Bedrock 的使用条款。

## 相关资源

- [Scalebox 文档](https://docs.scalebox.dev)
- [AWS Bedrock 文档](https://docs.aws.amazon.com/bedrock/)
- [DeepSeek 模型介绍](https://platform.deepseek.com/)

## 贡献

欢迎提交 Issue 和 Pull Request！