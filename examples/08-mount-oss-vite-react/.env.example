# Scalebox API 配置
SCALEBOX_API_KEY=your_scalebox_api_key_here
SCALEBOX_API_URL=https://api.scalebox.dev

# S3 / OSS 配置
# AWS S3：不需要设置 S3_ENDPOINT，SDK 会自动根据 REGION 选择
# S3_ENDPOINT=  # 留空或注释掉

# MinIO: S3_ENDPOINT=http://localhost:9000
# 阿里云 OSS: S3_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com

S3_ACCESS_KEY=your_s3_access_key
S3_SECRET_KEY=your_s3_secret_key
S3_BUCKET=your-bucket-name
S3_REGION=us-east-1  # 改为你的 bucket 所在的实际 region，如 us-east-2

# OSS 中的项目路径（相对于 S3_FOLDER）
# 完整路径为: s3://bucket/S3_FOLDER/PROJECT_PATH/
S3_FOLDER=frontend
PROJECT_PATH=vite-react

# 可选：指定 Scalebox 模板名称
# TEMPLATE_NAME=nodejs-24-nvm
